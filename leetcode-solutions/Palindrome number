class Solution {
    public boolean isPalindrome(int x) {
      int r = 0,t;
     
      int k = x;
      while(x>0){
      t = x%10;
      r = r*10 + t;
      x = x /10;

      }
      if (r==k){
          return true;
      }
      else{
          return false;
      }
    }
}
